@model mediabalans_sufre.Models.BLL.Resource

@{
    ViewData["Title"] = "Create";
}
<div class="row">
    <div class="col-md-6 my-4 mx-auto">
        <div class="card shadow mb-4">
            <div class="card-header py-4">
                Çeviri Ekle <a asp-action="Index" class="float-right btn btn-outline-secondary">Geri Git</a>
            </div>
            <div class="card-body">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="PageKey" class="control-label">Sayfa</label>
                        <input asp-for="PageKey" class="form-control" />
                        <span asp-validation-for="PageKey" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Key" class="control-label">Çeviri Key</label>
                        <input asp-for="Key" class="form-control" />
                        <span asp-validation-for="Key" class="text-danger"></span>
                    </div>
                    <div class="form-language my-3">
                        @{
                            int a = 0;
                            int b = 0;
                        }
                        <ul class="nav nav-tabs" id="myTab" role="tablist">
                            @foreach (var item in context.Languages)
                            {
                                <li class="nav-item">
                                    <a class="nav-link @(a == 0 ? "active":" ") " id="nav-@item.Code-tab" data-toggle="tab" href="#nav-@item.Code" role="tab" aria-controls="nav-@item.Code" aria-selected="true">@item.Name</a>
                                </li>
                                a++;
                            }
                        </ul>
                        <div class="tab-content" id="myTabContent">
                            @foreach (var item in context.Languages)
                            {
                                <div class="tab-pane fade @(b == 0 ? " show active" : "") py-4" id="nav-@item.Code" role="tabpanel" aria-labelledby="nav-@item.Code">
                                    <div class="form-group">
                                        <label class="control-label">Açıklama - (@item.ShortName)</label>
                                        <textarea class="form-control" rows="6" cols="6" name="translates[@b].Value"></textarea>
                                    </div>
                                    <input type="hidden" name="translates[@b].LanguageId" value="@item.Id" />
                                </div>
                                b++;
                            }
                        </div>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Ekle" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>